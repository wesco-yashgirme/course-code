name: checkout
on: [push]

jobs:
    simple:
        runs-on: ubuntu-latest
        steps:
            - name: simple json action
              uses: actions/hello-world-javascript-action@v1
            