name: checkout
on: [push]

jobs:
    simple:
        runs-on: ubuntu-latest
        steps:
            - name: simple json action
              uses: action/hello-world-javascript-action@main
            